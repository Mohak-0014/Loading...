/**
 * PhotoCapture
 * Shared camera input. Wraps <input capture='environment'>, downscales via utils/image.js, returns dataUrls. Used by report flow and fault lookup.
 */
export default function PhotoCapture(props) {
  // TODO
  return null
}
