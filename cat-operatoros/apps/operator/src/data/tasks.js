/**
 * Today's schedule. Mutated live by completeTask / demo scenarios.
 * Each task carries the context the ETA model needs as features.
 */
export const TASKS = [
  // { id, time, type, title, zone, status, cycles: {done, total},
  //   estimatedMin, predictedMin, slopeDeg, loadClass, travelMeters }
  // TODO(Block 2): 5 tasks, per docs/PRD.md section "Tasks"
]
